NOMBRE D'EMPLACEMENTS PRODUITS (120 vs 121) — [catalogue-odoo] §8.4 affirme « Le squelette contient 121 emplacements (Image0..Image120) » ; [ui-selection] §2.1 affirme « exactement 480 contrôles », « Image0…Image119 », « Max 120 produits par catégorie ». VERIFIE : C:\_dev\balance\Balance_Sauvegarde.mdb.src\forms\FormulaireSquelette.form contient 120 `Name ="ImageN"` (index max = 119), 120 LabelPrix, 120 LabelRef, 120 LabelDescription ; le .cls contient 120 `ImageN_Click` (index max 119). => ui-selection a raison, catalogue-odoo a tort. La règle métier réelle est « au-delà du 120e produit d'une catégorie, les produits sont perdus silencieusement ». Note : ui-selection se contredit lui-même en écrivant « 121 × (Image_i_Click, …) » pour FormulaireProduitsapparentes.cls alors qu'il relève par ailleurs que ce fichier a le même MD5 que FormulaireSquelette.cls.

---

EnvoiMail2emeEssai EST APPELEE — [admin-config] §9 la classe en « Fonctions mortes : EnvoiMail2emeEssai (jamais appelée : EnvoiMail retourne False sans qu'aucun appelant ne relance la 2ᵉ tentative) ». VERIFIE FAUX : C:\_dev\balance\Balance_Sauvegarde.mdb.src\modules\Module1.bas:1316 → `If EnvoiMail = False Then EnvoiMail2emeEssai`, dans ChargerFichierOdoo, sous `If OptionMailIntegrite = "O" Then / If savepos Then`. [catalogue-odoo] §7.5 et [exploitation-logs] §5.5 décrivent correctement ce chemin. Le retry 2 essais existe donc bien pour le mail d'intégrité comme pour les autres.

---

DECOMPTE DES CHAMPS DE Systeme SERIALISES PAR ListeParametresSysteme — [module-global] tableau des procédures : « ListeParametresSysteme | 291 | concaténation manuelle des 87 colonnes » ; [exploitation-logs] §6.6 : « LISTEPARAMETRESSYSTEME | mail avec les ~121 champs de Systeme (Module1.bas:5748) » et cite `Rs.Fields(120)` pour VersionApplication (§8), alors que [module-global] cite `Rs.Fields(78)` pour la même donnée. Les deux index peuvent coexister (InitTableSysteme et ListeParametresSysteme sont deux requêtes distinctes) mais le décompte 87 vs ~121 est incompatible et non tranché. Contexte vérifié : tbldefs\Systeme.sql compte 137 colonnes, dont 87 seulement sont chargées en globales gSysteme* par InitTableSysteme.

---

PORTEE REELLE DE Decimales_Poids — [balance-serie] §8 et [admin-config] §8B présentent Decimales_Poids (1..5) comme une règle métier active, en reprenant la table d'exemples de FAideDecimalesPoids (1,236 kg → 0493021012365 / 0493021012303 / 0493021012402) ; [etiquette-impression] §3.5 et bug E démontrent qu'il est SANS EFFET sur ~90 % des pesées. VERIFIE : dans C:\_dev\balance\Balance_Sauvegarde.mdb.src\forms\FormulaireCalcul.cls, `Reformate_Poids_Avec_Param` n'apparaît qu'en l.3288 (`PoidsPourValidation`, utilisée uniquement pour les tests de seuils), tandis que le code-barres est construit en l.3454-3455 avec `Poids_sansvirgule = Replace$(LabelPoidsBandeau.Caption, ",", "")` — poids BRUT balance. Aucune occurrence de `Decimales_Poids` ni `Decimales_Prix` dans tout FormulaireCalcul.cls. La contradiction porte sur LA règle centrale du système (ce qui est encodé dans le code-barres) et doit être tranchée avant réécriture. Idem pour CodeBarre_PrixouPoids : `Dim ValeurPrixouPoidsDansCodeBarre As String` (l.3393, LOCAL) puis auto-affectation l.3401 → toujours "" → branche « Poids » systématique sauf préfixes 0491/0492 (confirmé).
